# Step1 

# Use the following command to add the GNS3 repository, 
# then input your user password and press ENTER to continue.
`sudo add-apt-repository ppa:gns3/ppa`

# step2  
# preparing to install GNS3 GUI and Server and allow non-root users to use wireshark
# and bridge 

# step3
# the commands below is to install IOU packages and use its features and accept the instalation prompt by pressing y key

`sudo dpkg --add-architecture i386`

`sudo apt update`

`sudo apt install gns3-iou`

# IOU is an internal Cisco tool for simulating the ASICs in Cisco switches


# step4 
# remove docker old versions and install docker CE


`sudo apt update`
`sudo apt install docker-ce`

Add the appropriate repo:

sudo add-apt-repository \
"deb [arch=amd64] https://download.docker.com/linux/ubuntu \
   $(lsb_release -cs) stable"

# install docker-CE 

`sudo apt update`
`sudo apt install docker-ce`

# Finally, add your user to the following groups:

`ubridge libvirt kvm wireshark docker`

# use “sudo usermod -aG group user_name” to add your user to each of those groups) Restart your user session by logging out and back in, or restarting the system.



